# Canonical development dependency set for ITAMbox contributors and CI.
#
# `pip install -r requirements-dev.txt` from the repository root gives you
# everything needed to run the app, the test suite, and the lint/pre-commit
# checks. Runtime-only installs (e.g. production images) should use
# `itambox/requirements.txt` instead — see DEVELOPMENT.md.

-r requirements.txt

# Test runner (see .github/workflows/ci.yml "test" job)
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
pytest-cov>=5.0,<6.0
pytest-xdist>=3.5,<4.0

# Test fixtures / data generation
model_bakery>=1.17,<2.0
factory_boy>=3.3,<4.0
tblib>=3.0,<4.0

# Lint (see .github/workflows/ci.yml "lint" job and .pre-commit-config.yaml).
# flake8-bugbear supplies the B/B9 codes in setup.cfg's [flake8] select --
# without it, `select = B,C,E,F,W,B9` silently matches nothing for those
# prefixes instead of failing, so it must be installed alongside flake8, not
# just declared as a pre-commit additional_dependency.
# These versions are exact because scripts/flake8_baseline.json is derived
# from their diagnostics. Updating any of them requires regenerating and reviewing
# the baseline in the same PR.
flake8==7.0.0
flake8-bugbear==24.12.12
mccabe==0.7.0
pycodestyle==2.11.1
pyflakes==3.2.0

# Git hook runner for `make lint` / `make setup`
pre-commit>=4.0,<5.0

# Local-only debugging aid; never enabled in prod (see core/settings)
django-debug-toolbar>=4.4,<5.0
